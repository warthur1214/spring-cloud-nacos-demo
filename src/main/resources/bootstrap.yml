
spring:
  application:
    name: spring-cloud-nacos-demo
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      username: warthur
      password: yongqiang
      discovery:
#        server-addr: nacos.warthur.com:80
        server-addr: localhost:8848
        group: DEFAULT_GROUP
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
        namespace: ${spring.cloud.nacos.discovery.namespace}
        extension-configs:
          - refresh: true
            dataId: redis.yaml
#          - refresh: true
#            dataId: jdbc.yaml
          - refresh: true
            dataId: dubbo.yaml
          - refresh: true
            dataId: logback.yaml
          - refresh: true
            dataId: seata.yaml
          - refresh: true
            dataId: mybatis.yaml
          - refresh: true
            dataId: sa-token.yaml
          - refresh: true
            dataId: quartz.yaml
        group: ${spring.cloud.nacos.discovery.group}
  shardingsphere:
    datasource:
      names: ds0,ds1
      ds0:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://139.9.170.157:3306/test?useSSL=false&autoReconnect=true&allowMultiQueries=true&nullNamePatternMatchesAll=true
        username: root
        password: root@huaweiyun140
      ds1:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://139.9.170.197:3306/test?useSSL=false&autoReconnect=true&allowMultiQueries=true&nullNamePatternMatchesAll=true
        username: root
        password: root@huaweiyun140
    masterslave:
      name: ms
      master-data-source-name: ds0
      slave-data-source-names: ds1
      load-balance-algorithm-type: round_robin
    props:
      sql.show: true